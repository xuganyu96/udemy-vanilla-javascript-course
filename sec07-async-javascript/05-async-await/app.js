// async can be applied to a function to make it return a Promise that resolves to the thing that the function will 
// be returning without the async applied;
//
// await can be applied to a Promise that will make the runtime wait for the Promise to be resolved before executing 
// subsequent code.